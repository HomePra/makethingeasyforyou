 using TShark to filter out the User-Agent strings == tshark --Y http.request -T fields -e http.host -e http.user_agent -r analysis_file.pcap
sudo lvextend -r -l +100%FREE /dev/mapper/ubuntu--vg-ubuntu--lv == This command will extend the logical volume to use all available free space.
